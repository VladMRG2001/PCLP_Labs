#include <stdio.h>
#include <stdlib.h>

int main()
{
    int vector[100], i, n, minim, maim, pozmin, pozmax;
    printf ("Sa se citeasca numarul de elemente ale vectorului: ");
    scanf ("%d", &n);
    printf ("Sa se citeasca elementele vectorului: ");
    for (i=0;i<n;i++)
    {
        scanf ("%d", &vector[i]);
    }

}
